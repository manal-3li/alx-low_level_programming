#include "main.h"
/**
 * _isupper - checks for uppercase character.
 *@m: Variable that stores the letters
 * Return: Always 0.
 */
int _isupper(int m)
{
	if (m >= 'A' && m <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}

}
